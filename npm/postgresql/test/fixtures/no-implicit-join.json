{
  "__generated": {
    "source": "eslint-plugin-postgresql",
    "ref": "v0.22.1",
    "sourceDir": "tests/fixtures/no-implicit-join",
    "license": "MIT",
    "tool": "tools/tasks/sync-postgresql-tests.ts"
  },
  "valid": [
    {
      "name": "explicit-join",
      "code": "SELECT a.id FROM a JOIN b ON a.id = b.id;\n"
    },
    {
      "name": "single-table",
      "code": "SELECT id FROM a;\n"
    }
  ],
  "invalid": [
    {
      "name": "comma-join",
      "code": "SELECT a.id FROM a, b WHERE a.id = b.id;\n",
      "errors": [
        {
          "messageId": "noImplicitJoin",
          "message": "Comma-separated tables in `FROM` are an implicit cross join. Use explicit `JOIN ... ON ...` so the join condition lives next to the join.",
          "line": 1,
          "column": 1,
          "endLine": 1,
          "endColumn": 40
        }
      ],
      "output": null
    }
  ]
}
