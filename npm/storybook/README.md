# @oxlint-plugins/oxlint-plugin-storybook

Rust/Oxc-backed Oxlint plugin port of `eslint-plugin-storybook`.

The native core implements Storybook CSF, interaction, addon, import, and meta
rules with a thin JavaScript adapter for Oxlint's ESLint-compatible plugin API.
