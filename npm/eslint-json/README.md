# @oxlint-plugins/oxlint-plugin-eslint-json

Rust-backed Oxlint plugin port of `@eslint/json`.

This package exposes the `json` plugin namespace and a native API for scanning
JSON, JSONC, and JSON5-style source text.
