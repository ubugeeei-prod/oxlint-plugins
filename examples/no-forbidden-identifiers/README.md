# no-forbidden-identifiers

Example usage for `@oxlint-plugins/oxlint-plugin-no-forbidden-identifiers`.

From the repository root:

```sh
vp build
node examples/no-forbidden-identifiers/js-api.cjs
```

The `oxlint.config.jsonc` file shows the intended published package configuration shape.
