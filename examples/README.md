# Examples

These examples are intentionally small and are not npm workspaces.

- `no-forbidden-identifiers`: sample Oxlint JS plugin configuration and direct JS API usage.
- `port-prompt`: sample `vp run new` invocation for generating an agent handoff prompt.

Run examples from the repository root after `vp install` and `vp build`.
