# Port Prompt

Generate an AI handoff prompt for a full plugin:

```sh
vp run new eslint-plugin-react
```

Generate a prompt for one rule:

```sh
vp run new eslint-plugin-react/no-array-index-key
```

Generated prompts are written under `prompts/generated/`, which is intentionally ignored.
